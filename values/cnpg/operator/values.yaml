# CNPG Operator - minimal config
# The operator manages PostgreSQL clusters via Cluster CRDs

monitoring:
  podMonitorEnabled: true

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 256Mi
